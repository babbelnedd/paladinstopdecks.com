<script>window.uid = '{{_auth.uid}}';</script>
{{#ifEqual static.production true}}
    <script>
        /*function useAdBlock($c) {

            if ($c.length === 0) {
                return true;
            }
            if ($c.css('display') === 'none') {
                return true;
            }
            if ($c.css('visibility') === 'hidden') {
                return true;
            }

            return false;

        }*/

        (function (i, s, o, g, r, a, m) {
            i['GoogleAnalyticsObject'] = r;
            i[r] = i[r] || function () {
                        (i[r].q = i[r].q || []).push(arguments)
                    }, i[r].l = 1 * new Date();
            a = s.createElement(o), m = s.getElementsByTagName(o)[0];
            a.async = 1;
            a.src = g;
            m.parentNode.insertBefore(a, m)
        })(window, document, 'script', '//www.google-analytics.com/analytics.js', 'ga');

        ga('create', {
            trackingId: 'UA-70481300-1',
            cookieDomain: 'auto',
            siteSpeedSampleRate: 100
        });
        if (window.uid !== '') {
            ga('set', '&uid', window.uid);
        }
        ga('require', 'displayfeatures');
        // ga('require', 'linkid');

/*        if (useAdBlock($('#ad-container'))) {
            ga('set', 'dimension1', 'AdBlock');
        }*/

        ga('send', 'pageview');

        // todo: minfy pageview location on pages like /account/<ACCOUNTNAME> to '/account/ACCOUNTNAME

        window.onload = function () {
            if (window.performance) {
                ga('send', 'timing', 'COMPLETE SITE', 'load', performance.now());
            }
        };

        window.onerror = function (msg, file, line, col, err) {
            ga('send', 'exception', {
                'exDescription': err.stack,
                'exFatal': false
            });
        };


    </script>
{{/ifEqual}}